---
const steps = [
  {
    number: 1,
    title: 'Choose Your Starting Node',
    description: 'Select a concept that interests you. This becomes your entry point into the knowledge graph.'
  },
  {
    number: 2,
    title: 'Explore Connections',
    description: 'Discover how concepts relate to each other. Follow pathways that match your curiosity and goals.'
  },
  {
    number: 3,
    title: 'Master and Expand',
    description: 'Complete modules, unlock new nodes, and watch your knowledge graph grow into something uniquely yours.'
  }
]
---

<section class="py-24 bg-slate-50">
  <div class="max-w-7xl mx-auto px-6">
    <div class="text-center mb-20">
      <h2 class="text-5xl font-bold text-slate-900 mb-6">
        Your Learning, Visualized
      </h2>
      <p class="text-xl text-slate-600 max-w-2xl mx-auto">
        Navigate through a universe of knowledge, where each step forward unlocks new possibilities.
      </p>
    </div>

    <div class="grid md:grid-cols-2 gap-12 items-center">
      <!-- Steps -->
      <div class="space-y-8">
        {steps.map((step) => (
          <div class="flex gap-6">
            <div class="flex-shrink-0 w-12 h-12 bg-primary-600 rounded-full flex items-center justify-center text-white font-bold text-xl">
              {step.number}
            </div>
            <div>
              <h3 class="text-2xl font-bold text-slate-900 mb-2">{step.title}</h3>
              <p class="text-slate-600">{step.description}</p>
            </div>
          </div>
        ))}
      </div>

      <!-- Visualization -->
      <div class="relative">
        <div class="aspect-square rounded-2xl bg-gradient-to-br from-primary-100 to-primary-200 p-8 flex items-center justify-center">
          <div class="relative w-full h-full">
            <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-20 h-20 bg-primary-600 rounded-full flex items-center justify-center text-white font-bold shadow-xl z-10">
              You
            </div>
            <div class="absolute top-8 left-8 w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg animate-float">
              <div class="w-8 h-8 bg-primary-300 rounded-full"></div>
            </div>
            <div class="absolute top-8 right-8 w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg animate-float animation-delay-1000">
              <div class="w-8 h-8 bg-blue-300 rounded-full"></div>
            </div>
            <div class="absolute bottom-8 left-8 w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg animate-float animation-delay-2000">
              <div class="w-8 h-8 bg-primary-400 rounded-full"></div>
            </div>
            <div class="absolute bottom-8 right-8 w-16 h-16 bg-white rounded-full flex items-center justify-center shadow-lg animate-float animation-delay-3000">
              <div class="w-8 h-8 bg-blue-400 rounded-full"></div>
            </div>
            <svg class="absolute inset-0 w-full h-full" xmlns="http://www.w3.org/2000/svg">
              <line x1="50%" y1="50%" x2="20%" y2="15%" stroke="#0284c7" stroke-width="2" opacity="0.3" stroke-dasharray="5,5"/>
              <line x1="50%" y1="50%" x2="80%" y2="15%" stroke="#0284c7" stroke-width="2" opacity="0.3" stroke-dasharray="5,5"/>
              <line x1="50%" y1="50%" x2="20%" y2="85%" stroke="#0284c7" stroke-width="2" opacity="0.3" stroke-dasharray="5,5"/>
              <line x1="50%" y1="50%" x2="80%" y2="85%" stroke="#0284c7" stroke-width="2" opacity="0.3" stroke-dasharray="5,5"/>
            </svg>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>